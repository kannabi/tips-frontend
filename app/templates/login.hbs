<div class="main-container fullscreen">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-xl-5 col-lg-6 col-md-7">
        <div class="text-center">
          <h1 class="h2">Вход в систему</h1>
          <p class="lead">Войдите в аккаунт, используя учетные данные</p>
          <form {{action 'authenticate' on='submit'}}>
            <div class="form-group">
              {{input class="form-control" type="text" placeholder="Логин" name="login" value=login}}
            </div>
            <div class="form-group">
              {{input class="form-control" type="password" placeholder="Пароль" name="password" value=password}}
            </div>
            <button class="btn btn-lg btn-block btn-primary" role="button" type="submit">
              Войти
            </button>
            <small>Еще нет аккаунта? {{#link-to "register"}}Создайте{{/link-to}}!</small>
          </form>
          <div class="alert alert-danger mt-2 {{unless errorMessage 'invisible'}} login-error-message-wrapper" role="alert">
            {{errorMessage}}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
